# File: ytget_gui/_version.py

from __future__ import annotations

__version__ = "2.8.1"

APP_NAME = "YTGet"
ORG_NAME = "YTGet"
ORG_DOMAIN = "ytget_gui.local"
# Stable across releases on purpose. Windows keys the taskbar icon, the jump
# list and shortcut grouping to this string, so a version-specific ID made
# Explorer look up an identity it had never seen before -- which is why the
# first launch after a boot showed the generic placeholder icon.
APP_USER_MODEL_ID = "ErfanNamira.YTGet"

GITHUB_URL = "https://github.com/ErfanNamira/ytget-gui"
GITHUB_OWNER = "ErfanNamira"
GITHUB_REPO = "ytget-gui"
